# Docker

## Docker

docker images
docker containe ps

## Docker compose

docker-compose stop
docker-compose start

docker-compose up -d

docker-compose down

Delete stopped containers
docker-compose rm
