# Docker

## Docker

```Shell
docker images
docker containe ps
```

## Docker compose

```Shell
docker-compose stop
docker-compose start

docker-compose up -d

docker-compose down

Delete stopped containers
docker-compose rm
```
